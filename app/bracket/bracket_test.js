'use strict';

describe('myApp.bracket module', function() {

  beforeEach(module('myApp.bracket'));

  describe('bracket controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var bracketCtrl = $controller('bracketCtrl');
      expect(bracketCtrl).toBeDefined();
    }));

  });
});